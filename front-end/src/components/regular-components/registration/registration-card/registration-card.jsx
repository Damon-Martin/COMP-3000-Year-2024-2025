export default function RegistrationCard({ width = "30vw", height = "30vh", margin = "20px" }) {
    return (
        <form
            style={{
                width: width,
                height: height,
                margin: margin,
                backgroundColor: "#D9D9D9",
                borderRadius: "0.375rem", 
            }}
        >
            {/* Form contents */}
        </form>
    );
}
